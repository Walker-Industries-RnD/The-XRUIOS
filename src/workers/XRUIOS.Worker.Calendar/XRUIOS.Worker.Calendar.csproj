<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <OutputType>Exe</OutputType>
    <RootNamespace>XRUIOS.Worker.Calendar</RootNamespace>
    <AssemblyName>XRUIOS.Worker.Calendar</AssemblyName>
  </PropertyGroup>

  <!-- This worker compiles ONLY the Calendar system code. Refs + packages come from Directory.Build.props. -->
  <ItemGroup>
    <Compile Include="..\..\systems\CalendarClass.cs" />
  </ItemGroup>

</Project>
